##Spring Configuration
server:
  port: 8080
spring:
  application:
    name: ms-banking-system
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: /h2
  profiles:
    include:
